spring.application.name=biz-service-admin

#映射到Config客户端的spring.profiles.active属性，可以用来区分环境
spring.cloud.config.profile=native
#映射到Git服务器的commit id, 分支名称或者tag，默认值为master
spring.cloud.config.label=master
#映射到Config客户端的spring.application.name属性
#spring.cloud.config.application=biz-service-a
spring.cloud.config.uri=http://localhost:7001/
