server:
  port: 30010
spring:
  application:
    name: consumer
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10010/eureka/

#开启feign的熔断降级机制
feign:
  hystrix:
    enabled: true
