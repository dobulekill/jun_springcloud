spring:
  application:
    name: sms-platform-cache
  cloud:
    config:
      discovery:
        enabled: true
        service-id: SMS-PLATFORM-CONFIG-SERVER
      label: master
      profile: '@profileActive@'

  profiles:
    active: '@profileActive@'